/**
 * `risk` formatter — A5 (Surface), issue #52 / Part M. PHASE 0 STUB.
 * TerminalFormatter.risk (why.format.ts) delegates here.
 */

import type { RiskReport } from '../../core/index.js';
import { NotImplemented } from '../../core/index.js';

export function formatRisk(_report: RiskReport): string {
  throw new NotImplemented('formatRisk (#52)');
}
