/**
 * Benchmark report rendering — A7 (Auditor), issue #62 / Part H.3. PHASE 0 STUB.
 * Printed by `pnpm bench`; attached to every `provenance/` PR description (Part I.4).
 */

import type { Metrics } from './metrics.js';
import { NotImplemented } from '../core/index.js';

export function renderReport(_metrics: Metrics): string {
  throw new NotImplemented('renderReport (#62)');
}
