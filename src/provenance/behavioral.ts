/**
 * Behavioral evidence — A2 (Tracer), issue #45 / Part D.6. PHASE 0 STUB.
 * Co-changed paths + short structural hints from the introducing diff. No LLM.
 */

import type { BehavioralEvidence, CommitDiff } from '../core/index.js';
import { NotImplemented } from '../core/index.js';

export function extractBehavioralEvidence(_diff: CommitDiff): BehavioralEvidence {
  throw new NotImplemented('extractBehavioralEvidence (#45)');
}
